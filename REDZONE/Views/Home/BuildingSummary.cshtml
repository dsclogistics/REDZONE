@model REDZONE.Models.BuildingSummaryViewModel1
@{
    ViewBag.Title = "Building Summary";
    int colCounter = 0;
}
<link href="~/Content/RedZoneApp.css" rel="stylesheet" />
<link href="~/Content/BuildingSummary.css" rel="stylesheet" />
<link href="~/Content/RZ_Effects.css" rel="stylesheet" />
<script src="~/Scripts/aes.js"></script>
<script src="~/Scripts/aes-ctr.js"></script>
<script src="~/Scripts/BuildingSummary.js"></script>
<style>
    .divCell {
        display: table-cell;
        vertical-align: middle;
    }
</style>

<div class="row" style="font-family:Arial; margin-top: 5px">
    <div>
        <input type="hidden" id="buildingName" value="@Model.bName" />
        <input type="hidden" id="buildingYear" value="@Model.year" />
        <input type="hidden" id="buildingId" value="@Model.bId" />
        <input type="hidden" id="cellIdSelected" value="" />
        <input type="hidden" id="reviewerMetrics" value="@ViewBag.ReviewerMetrics" />
    </div>
    <div class="panel panel-default" style="margin-bottom:0px; ">
        <div class="panel-heading" style="text-align:center; padding: 0px 15px; ">
            <div class="row">
                <div class="col-sm-2" style="text-align:left; padding:0; padding-left:10px">
                    @if (Model.canFilterBuildings)
                    {
                    <div style="margin:0; padding:0; margin-top:38px">
                        <label class="switch" style="display:inline-block; margin:0;">
                            <input class="switch-input" id="chkMyBuildingsOnly" type="checkbox" @Model.applyFilter />
                            <span class="switch-label" data-on="Y" data-off="N"></span>
                            <span class="switch-handle"></span>
                        </label>
                        <span class="" style="font-size:12px; margin:0; padding:0; padding-top:5px; display:inline-block">View my Buildings Only</span>
                    </div>

                    @*<div class="checkbox" style="padding-left:45px; padding-top:76px">
                            <label class="" style="text-align:left; padding:0; font-size: .8em">
                                <input id="chkMyBuildingsOnly" type="checkbox" value="" @applyFilter>
                                <span class="cr"><i class="cr-icon glyphicon glyphicon-ok" style="color: hsl(240, 45%, 40%); font-weight:bold"></i></span>
                                <span id="chkText" style="font-size:12px">View my Buildings Only</span>
                            </label>
                        </div>*@
                    }
                </div>
                <div class="col-sm-8">
                    <div class="row ">
                        <span style="font-size:x-large; font-weight:bold; color:darkblue">Building Summary</span>
                    </div>
                    @* <----------- Navigation Field by Building*@
                    <div class="row" style="margin-bottom:3px">
                        <div class="input-group col-sm-10 col-sm-offset-1" style="padding:0px; ">
                            <span class="input-group-btn">
                                <button id="btn-lastmonth" type="button" class="btn btn-primary" style="float:right; width:60px" onclick="location.href='@Model.urlPrevBuilding'" title="Navigate to Previous Building" @Model.statusPrevBuilding>
                                    <span class="glyphicon glyphicon-chevron-left"></span>
                                </button>
                            </span>
                            <input type="text" class="form-control" value="@Model.bName" readonly="readonly" style="min-width:100%; text-align:center; font-weight:bold; font-size:x-large; padding:0px">
                            <span class="input-group-btn">
                                <button id="btn-nextmonth" type="button" class="btn btn-primary" style="float:right; width:60px" onclick="location.href='@Model.urlNextBuilding'" title="Navigate to Next Building" @Model.statusNextBuilding>
                                    <span class="glyphicon glyphicon-chevron-right"></span>
                                </button>
                            </span>
                        </div>
                    </div>@* <----------- End of Navigation Field by Building*@
                    @* <----------- Navigation Field by Year *@
                    <div class="row" style="margin-bottom:10px; padding:0px;">
                        <div class="input-group col-sm-10 col-sm-offset-1" style="padding:0px;">
                            <span class="input-group-btn" style="padding:0px;">
                                <button id="btn-lastmonth" type="button" class="btn btn-primary" style="float:right; width:60px;" onclick="location.href='@Model.urlPrevPeriod'" title="Navigate to Previous Building" @Model.statusPrevPeriod>
                                    <span class="glyphicon glyphicon-chevron-left"></span>
                                </button>
                            </span>
                            @*<span style="min-width:100%; text-align:center; vertical-align:middle; font-size:large; padding:0px">Period: @Model.year</span>*@
                            <input type="text" class="form-control" value="@Model.year" readonly="readonly" style="min-width:100%; text-align:center; font-weight:bold; font-size:large; padding:0px">
                            <span class="input-group-btn">
                                <button id="btn-nextmonth" type="button" class="btn btn-primary" style="float:right; width:60px;" onclick="location.href='@Model.urlNextPeriod'" title="Navigate to Next Building" @Model.statusNextPeriod>
                                    <span class="glyphicon glyphicon-chevron-right"></span>
                                </button>
                            </span>
                        </div>
                    </div>@* <----------- End of Navigation Field by Year *@
                </div>
                <div class="col-sm-2">
                    @*<input type="image" src="~/Images/BackToDashboard.png" width="80" height="35" style="float:right" onclick="javascript:location('/Home/Index')">*@
                </div>
            </div>
        </div> <!-- END OF PANEL HEADER -->

        <div class="panel-body" style="margin:2px; padding:5px; background-color: #fefefe">
          @if (Model.metricRows.Count == 0)
          {
              <div style="color:red; padding: 50px 5px; font-weight:bold">
                   THERE IS NO METRICS DATA AVAILABLE FOR THE<br />
                   SELECTED PERIOD / BUILDING CONBINATION
              </div>
          }
          else if (Model.isModelValid)
          {
            <div>
                <input type="hidden" id="viewMode" value="byMonth" />
            </div>
            <div class="row" id="viewByMonth" style="border:1px solid lightgray; border-radius: 5px; margin: 0; padding:0; ">
                <table class="metricTable" style="width:100%;">
                    <tr class="rowHeader">
                        <!-- ******* HEADERS/TITLES ROW ********* -->
                        <td style="width:16.66%">
                            <span style="font-weight:bold"> @Model.buildingHeadings.rowName</span>
                            <span class="btnFlip" style="float:right"><img src="~/Images/ChangeView.png" /></span>
                        </td>
                        <td style="width:8.33%; background-color: #bbedff">
                            Goal
                        </td>
                        @{colCounter = 0;}
                        @foreach (REDZONE.Models.MeasuredCellEntity dscBldngHdr in @Model.buildingHeadings.entityMetricCells)
                        {
                            if (dscBldngHdr.isViewable)
                            {
                                <td style="width:@Model.metricColWidth">
                                    <input type="hidden" id="month-@colCounter.ToString("00")" value="@dscBldngHdr.metricMonth" />
                                    @dscBldngHdr.metricName
                                    @if (@dscBldngHdr.cellStatus.Equals("Mixed") || @dscBldngHdr.cellStatus.Equals("Open"))
                                    {
                                        <span style="color:red">*</span>
                                    }
                                </td>
                            }
                            colCounter++;
                        }
                        <td style="width:8.33%;">
                            Goals Missed
                        </td>
                    </tr>  <!-- END OF HEADERS/TITLES ROW -->
                    <!-- =========================================================================================================== -->
                    <!-- ================== Begin loop sections of Building Metric Rows ============================================ -->
                    @foreach (var buildingRow in Model.metricRows)
                    {
                        colCounter = 0;
                        <tr class="buildingRow">
                            <td style="width:16.66%; font-weight:bold">
                                <a href="@buildingRow.rowURL" id="mName" title="Metric Owner: @buildingRow.rowOwner">@buildingRow.rowName</a>
                            </td> <!-- Metric Name Column -->
                            <td style="width:8.33%; background-color: #bbedff; color:blue; font-weight:bold;">
                                <span id="mGoal">@HttpUtility.HtmlDecode(buildingRow.scoreGoal)</span>
                            </td> <!-- Goal Column -->
                            @foreach (REDZONE.Models.MeasuredCellEntity buildingCell in buildingRow.entityMetricCells)
                            {
                                bool cellIsViewable = Model.buildingHeadings.entityMetricCells[colCounter].isViewable;
                                if (cellIsViewable)
                                {
                                    string hasReasonsClass = (buildingCell.hasReasons) ? "note" : "";
                                    //string APstatusFlag = (buildingCell.rz_bapm_status != "") ? "*" : "";
                                    string APstatusFlag = (buildingCell.rz_bapm_status != "") ? "" : "";
                                    <td id="@buildingCell.cellValueId" class="valueCell @buildingCell.displayClass" style="padding:0; width:@Model.metricColWidth" title="Right Click any cell to Add/Edit Reasons and Action Plans [STS: @buildingCell.cellStatus - @buildingCell.rz_bapm_status]">
                                        <div class="@hasReasonsClass mvCell" style="margin:0; padding:4px; ">
                                            <input type="hidden" id="mpId" value="@buildingCell.mtrc_period_id" />
                                            <input type="hidden" id="mDisplayClass" value="@buildingCell.displayClass" />
                                            <input type="hidden" id="colIndex" value="month-@colCounter.ToString("00")" />
                                            <input type="hidden" id="bamp_status" value="@buildingCell.rz_bapm_status" />
                                            <input type="hidden" id="bapm_id" value="@buildingCell.rz_bapm_id" />
                                            <input type="hidden" id="hasReasons" value="@hasReasonsClass" />
                                            <input type="hidden" id="periodStatus" value="@buildingCell.cellStatus" />
                                            @*@Model.metricColWidth*@
                                            @if (String.IsNullOrEmpty(@buildingCell.metricValue))
                                            { <br /> }
                                            else
                                            { <span id="mValue">@buildingCell.metricValue </span><span style="color:blue">@APstatusFlag</span> }
                                        </div>
                                    </td>
                                }
                                colCounter++;
                            }
                            <td class="scoreCol @buildingRow.displayClass" style="width:8.33%; color:black; font-weight:bold">
                                @buildingRow.redTotals
                            </td>
                        </tr>
                    }  @*  <------- End of BuildingRow Rows Section ---- *@


                    <!--  ************ Start of the TOTALS ROW ******************  -->
                    <tr class="buildingRow totalsRow">
                        <td style="width:16.66%; padding:0; padding-top:4px">
                            @*@Model.buildingScoreRow.rowName*@
                            <span style="margin-top:10px; padding-top:10px">Building Score</span>
                            <button type="button" id="btnActionsRequired" class="btn  btn-sm" style="padding:0px; padding-left:5px; background-color:transparent; color: darkgreen " title="Show Actions Required by Building Score">
                                <span class="glyphicon glyphicon-question-sign" style="font-size:large;"></span>
                            </button>
                        </td><!-- Metric Name Column -->
                        <td style="width:8.33%; ">
                            <br />
                        </td><!-- Goal Column -->
                        @foreach (REDZONE.Models.MeasuredCellEntity buildingCell in Model.buildingScoreRow.entityMetricCells)
                        {
                            if (buildingCell.isViewable)
                            {
                                <td class="@buildingCell.displayClass" style="border-bottom: 1px solid lightgray; width:@Model.metricColWidth">
                                    @if (String.IsNullOrEmpty(@buildingCell.metricValue))
                                    { <br /> }
                                    else
                                    { @buildingCell.metricValue }
                                </td>
                            }
                        }
                        <td class="scoreCol" style="width:8.33%;">
                            <br />
                        </td>
                    </tr> <!-- End of Total Goals Missed Data Section -->

                </table>
            </div> <!-- End of DIV Table Table -->

            <!-- Render Partial View with Table ordered by Metrics on Top -->
            @Html.Partial("_BuildingSummaryMetric", Model.summaryByMetric)                 
              
        <!-- **************************** RECENT ACTION PLAN HISTORY SECTION *********************************************  -->
              <div id="APhistory" style="max-width:1000px; margin:0 auto">
                    @*Draw a Section for each Month in Reverse Order*@
                    @{
                            colCounter = 0;
                            bool isFirstMonth = true;
                    }
                    @for (int mIndex = Model.buildingHeadings.entityMetricCells.Count() - 1; mIndex >= 0; mIndex--)
                    {
                        REDZONE.Models.MeasuredCellEntity dscBldngHdr = Model.buildingHeadings.entityMetricCells[mIndex];
                        int monthReds = 0;
                        Int32.TryParse(@Model.buildingScoreRow.entityMetricCells[mIndex].metricValue, out monthReds);

                        string collapseStatus = isFirstMonth ? "in" : "";
                        string APglyphicon = isFirstMonth ? "glyphicon-chevron-down" : "glyphicon-chevron-right";

                        if (monthReds > 2)
                        {
                            string monthDivId = "monthDiv" + (colCounter + 1).ToString("00");

                            <div class="panel panel-default APmonthSection" style="font-family:Arial; max-width:1000px; margin:0 auto; margin-top:10px">
                                <input type="hidden" id="monthYearDIV" value="@dscBldngHdr.metricMonth @Model.year" />
                                <div class="panel-heading" style="text-align:left; padding-top:0px; padding-bottom:0px">
                                    <a class="glyph-link" data-toggle="collapse" href="#@monthDivId" style="text-decoration:none; color:black">
                                        <table style="width:100%;">
                                            <tr>
                                                <td style="width:65%; text-align: left; ">
                                                    <span class="glyphicon @APglyphicon"></span>
                                                    <span class="btn-group " style="text-align:left; margin:auto; margin-left:5px; font-size:18px; padding-top:5px; font-weight:bold ">
                                                        Action Plans for @dscBldngHdr.metricMonth @Model.year
                                                        @if (isFirstMonth)
                                                        {<span class="" style="color:red; margin-left:10px; font-size:14px; font-weight:normal"> [Action Plans are required]</span>}
                                                    </span>
                                                </td>
                                                <td style="width:35%">
                                                    @if (Model.buildingHeadings.entityMetricCells[mIndex].cellStatus.Equals("Mixed") || Model.buildingHeadings.entityMetricCells[mIndex].cellStatus.Equals("Open"))
                                                    {
                                                        <span style="float:right; color:red; margin-right:5px; font-weight:bold; padding-top:3px">* <span style="color:black; ">Metrics for this month have not been finalized</span></span>
                                                    }
                                                </td>
                                            </tr>
                                        </table>
                                    </a>
                                </div>

                                <div id="@monthDivId" class="panel-collapse collapse @collapseStatus">
                                    <table class="table table-condensed table-hover" style="text-align:center; margin-top:5px">
                                        <thead>
                                            <tr style="font-weight:bold; border-bottom:solid lightgrey 2px">
                                                <td style="width:20%; text-decoration:underline">Metric</td>
                                                <td style="width:15%; text-decoration:underline">Status</td>
                                                <td style="width:15%; text-decoration:underline">Action</td>
                                                <td style="width:15%; text-decoration:underline">Meeting Date</td>
                                                <td style="width:20%; text-decoration:underline">Meeting Owner</td>
                                                <td style="width:15%; text-decoration:underline">Last Update</td>
                                            </tr>
                                        </thead>

                                        @foreach (var buildingRow in Model.metricRows.OrderBy(x => x.rowName).ToList())
                                        {
                                            //if (@buildingRow.entityMetricCells[mIndex].rz_bapm_status == "Approved")
                                            if (!String.IsNullOrEmpty(@buildingRow.entityMetricCells[mIndex].rz_bapm_status))
                                            {
                                                <tr>
                                                    <td style="width:20%; font-weight:bold">@buildingRow.rowName</td>
                                                    <td style="width:15%">@buildingRow.entityMetricCells[mIndex].rz_bapm_status</td>
                                                    <td style="width:15%">
                                                        @if (@buildingRow.entityMetricCells[mIndex].nextCellAction.Equals("---"))
                                                        {
                                                            <span>@buildingRow.entityMetricCells[mIndex].nextCellAction</span>
                                                        }
                                                        else
                                                        {
                                                            <a class="lnkGotoAP" href="#" style="text-decoration:underline">
                                                                @buildingRow.entityMetricCells[mIndex].nextCellAction
                                                                <input id="bapm_id" type="hidden" value="@buildingRow.entityMetricCells[mIndex].rz_bapm_id" />
                                                                <input id="mpv_id" type="hidden" value="@buildingRow.entityMetricCells[mIndex].cellValueId" />
                                                                <input id="mp_id" type="hidden" value="@buildingRow.entityMetricCells[mIndex].mtrc_period_id" />

                                                                <input id="mpName" type="hidden" value="@buildingRow.entityMetricCells[mIndex].metricName" />
                                                                <input id="mpGoal" type="hidden" value="@buildingRow.scoreGoal" />
                                                                <input id="mpValue" type="hidden" value="@buildingRow.entityMetricCells[mIndex].metricValue" />
                                                                <input id="mpValueDisplayClass" type="hidden" value="@buildingRow.entityMetricCells[mIndex].displayClass" />
                                                            </a>
                                                        }
                                                    </td>
                                                    <td style="width:15%">N/A</td>
                                                    <td style="width:20%">@buildingRow.rowOwner</td>
                                                    <td style="width:15%">@buildingRow.entityMetricCells[mIndex].metricLastUpdt</td>
                                                </tr>
                                            }
                                        }

                                    </table>
                                </div>
                            </div>

                                        isFirstMonth = false;
                        }
                        colCounter++;
                    }
                </div>
              <br />
           }
           else
           {
              <div class="row" style="color:red; font-weight:bold; font-size:18px">
                  <span>There is no Building data to display.</span><br />
                  <span>Please go back to review your input and select a valid building.</span>
              </div>
           }
        </div> <!-- END OF PANEL BODY -->
    </div><!-- END OF PANEL -->
</div> <!-- ROW END -->
<!--  ************************************* START OF RIGHT CLICK CONTEXT MENU **************************************** -->
<nav id="context-menu" class="context-menu" style="border: 3px double gray">
    <ul id="cMenu" class="context-menu__items " style="padding:0; margin:0; color:darkgray">
        <li id="li_View" class="context-menu__item">
            <a href="#" class="context-menu__link" data-action="View">
                View Reasons
            </a>
        </li>
        <li id="li_Add" class="context-menu__item " style="display:none">
            <a href="#" class="context-menu__link" data-action="Add">
                Add Reasons
            </a>
        </li>
        <li id="li_Manage" class="context-menu__item">
            <a href="#" class="context-menu__link" data-action="Manage">
                Manage Reasons
            </a>
        </li>
        <li id="li_ViewAP" class="context-menu__item" style="display:none">
            <a href="#" class="context-menu__link" data-action="ViewAP">
                View Action Plan
            </a>
        </li>
        <li id="li_StartAP" class="context-menu__item" style="display:none">
            <a href="#" class="context-menu__link" data-action="StartAP">
                Start Action Plan
            </a>
        </li>
        <li id="li_ContinueAP" class="context-menu__item" style="display:none">
            <a href="#" class="context-menu__link" data-action="ContinueAP">
                Continue Action Plan
            </a>
        </li>
        <li id="li_ReviewAP" class="context-menu__item" style="display:none">
            <a href="#" class="context-menu__link" data-action="ReviewAP">
                Review Action Plan
            </a>
        </li>
        <li id="li_NoOptions" class="context-menu__item" style="display:none">
            <a href="#" class="context-menu__link" data-action="Nothing">
                No Actions Allowed for This Metric Value
            </a>
        </li>

        @*<li class="context-menu__item">
                   <a href="#" class="context-menu__link" data-action="Menu3">
                       <i class="fa fa-times"></i>  Menu Item 3
                   </a>
            </li>*@
    </ul>
</nav>  <!-- END OF RIGHT CLICK CONTEXT MENU -->
<!-- *************************** START OF RIGHT CLICK POPUP REASONS DISPLAY ******************************************  -->
<div id="popupViewReasons" class="modal fade" role="dialog">
    <div class="modal-dialog" style="width:50%; min-width:670px">
        <!-- Modal content-->
        <div class="modal-content">
            <div class="modal-header bg-Primary-Gradient">
                <button type="button" class="close" data-dismiss="modal">&times;</button>
                <h4 class="modal-title add-reason">Reasons for Selected Building Metric Value</h4>
            </div>
            <div class="modal-body" id="reasonEditBody">
                <div class="row puViewBody" id="reasonsViewContainer">

                    <div>
                        <br />PLEASE WAIT WHILE DATA LOADS<br /><br />
                        <img src="/Images/ui-anim_basic_16x16.gif" />
                        <br /><br />
                    </div>

                </div>
            </div>
            <div class="modal-footer" id="reasonEditFooter" style="text-align:center">
                <button type="button" class="btn btn-primary" id="btnCancelReasonEdit" data-dismiss="modal" style="width:120px">DONE</button>
            </div>
        </div>
    </div>
</div>
<!-- END OF RIGHT CLICK POPUP REASONS DISPLAY  -->
